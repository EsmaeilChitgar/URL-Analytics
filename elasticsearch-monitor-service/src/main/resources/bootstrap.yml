server:
  port: 8080
spring:
  application:
    name: elasticsearch-monitor-service
  cloud:
    config:
      uri: http://localhost:9090
      profile: default
      name: elastic
elasticsearch:
  rest:
    uris: http://localhost:9200